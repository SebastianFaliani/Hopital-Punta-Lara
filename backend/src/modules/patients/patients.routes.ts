import { Router } from 'express';import { authenticateToken,authorizeRoles } from '../auth/auth.middleware';import { handleCreatePatient,handleGetPatient,handleGetPatients,handleUpdatePatient } from './patients.controller';const router=Router();router.get('/',authenticateToken,authorizeRoles('admin','dir','lab','user'),handleGetPatients);router.get('/:id',authenticateToken,authorizeRoles('admin','dir','lab','user'),handleGetPatient);router.post('/',authenticateToken,authorizeRoles('admin','dir','lab'),handleCreatePatient);router.put('/:id',authenticateToken,authorizeRoles('admin','dir','lab'),handleUpdatePatient);export default router;
